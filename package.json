{
  "name": "swapduel",
  "private": true,
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --filter @swapduel/web generate",
    "dev": "pnpm --filter @swapduel/web dev",
    "dev:server": "pnpm --filter @swapduel/server dev",
    "start": "pnpm --filter @swapduel/server start",
    "test": "pnpm --filter @swapduel/game-engine test && pnpm --filter @swapduel/server test",
    "test:watch": "pnpm --filter @swapduel/game-engine test:watch",
    "conformance:generate": "vitest run tools/conformance/generate-fixtures.test.ts",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
